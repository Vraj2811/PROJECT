What is the drawback of computing the average of two numbers n and m as (n+m)/2 in a real-world computer? Devise a way to compute the same function that avoids this drawback. Assume that n and m are non-negative.
